# Secrets / tokens -- must never be committed
*token*
*.secret
*.secrets
.env
.env.*
*.pem
*.key
credentials*.json
service-account*.json

# Python build artifacts
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local build / editor / OS cruft
*.log
.DS_Store
.idea/
.vscode/

# Generated page — rebuilt by generator.py on every run and published
# straight from the Pages artifact. Tracking it causes push conflicts.
index.html

# design mockups for the V2.8 restructure -- throwaway, never shipped
prototype/
